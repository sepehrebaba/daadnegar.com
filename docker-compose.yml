# ONLY FOR DEVELOPMENT USAGE

services:
  #  app:
  #    build:
  #      context: .
  #      dockerfile: Dockerfile
  #    image: daadnegar-app:latest
  #    container_name: daadnegar_app
  #    restart: unless-stopped
  #    ports:
  #      - "3000:3000"
  #    environment:
  #      # Prisma migrate deploy (prestart) + app runtime
  #      DATABASE_URL: mysql://${DATABASE_USER}:${DATABASE_PASSWORD}@daadnegar_db:3306/${DATABASE_NAME}
  #      # MinIO
  #      MINIO_ENDPOINT: minio
  #      MINIO_PORT: "9000"
  #      MINIO_USE_SSL: "false"
  #      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
  #      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
  #      MINIO_BUCKET: daadnegar-uploads
  #      # RabbitMQ
  #      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
  #      # Auth
  #      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-dev-secret}
  #      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
  #    depends_on:
  #      daadnegar_db:
  #        condition: service_healthy
  #      minio:
  #        condition: service_healthy
  #      rabbitmq:
  #        condition: service_healthy
  #    networks:
  #      - daadnegar_network

  daadnegar_db:
    image: mysql:${DATABASE_VERSION:-8.0}
    container_name: daadnegar_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DATABASE_NAME}
      MYSQL_USER: ${DATABASE_USER}
      MYSQL_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - ${DATABASE_FORWARD_PORT:-3307}:3306
    volumes:
      - db_data:/var/lib/mysql
      - ./docker/mysql-init:/docker-entrypoint-initdb.d
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    networks:
      - daadnegar_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: quay.io/minio/minio:latest
    container_name: daadnegar_minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - daadnegar_network
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  rabbitmq:
    image: rabbitmq:3-management
    container_name: daadnegar_rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - daadnegar_network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  daadnegar_network:

volumes:
  db_data:
  minio_data:
